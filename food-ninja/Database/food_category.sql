-- ========================================================
-- Table: food_category
-- ========================================================

CREATE TABLE IF NOT EXISTS food_category (
  category VARCHAR(50) PRIMARY KEY,
  picture BYTEA
);
